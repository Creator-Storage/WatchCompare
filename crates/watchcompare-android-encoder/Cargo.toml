[package]
name = "watchcompare-android-encoder"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
links = "watchcompare-android-encoder"

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }

[dependencies]
serde.workspace = true
tauri = { version = "2" }
